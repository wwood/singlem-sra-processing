apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: sandpiper4
  region: us-east-1

managedNodeGroups:
  - name: io-unchunked-1
    instanceType: c7a.12xlarge
    minSize: 0
    maxSize: 100
    desiredCapacity: 0
    volumeSize: 350
    volumeType: gp3
    volumeIOPS: 6000
    volumeThroughput: 1000 # gp3 max; the .sylref pread traffic + SRA downloads share this one root volume, and 300 saturated it (VolumeThroughputExceeded)
    spot: true
